export const getGridItemStyle = (isPhone) => ({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  mt: isPhone ? '10%' : 0,
});

export const errorTitle = '404';
export const errorSubtitle = 'На жаль, сторінки, яку ви шукаєте, не існує.';
export const btnText = 'Головна сторінка';
